import React from "react";
import "./ChatIcon.css";

const ChatIcon = () => {
  return (
    <div className="chat-icon">
      <i className="fas fa-comment"></i>
    </div>
  );
};

export default ChatIcon;
